import { questionAnswer } from 'js-week3-cr-qa/helpers/question-answer';
import { module, test } from 'qunit';

module('Unit | Helper | question answer');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionAnswer([42]);
  assert.ok(result);
});
